<h1>ここがホントのbooks/show</h1>

<div>
  <h2>User info</h2>
  <div>
    <%= image_tag @user.get_profile_image(100,100) %>
  </div>
  <tbody>
    <tr>
      <th>name</th>
      <th><%= @user.name %></th><br>
    </tr>
    <tr>
      <th>introduction</th>
      <th><%= @user.introduction %></th>
    </tr>
  </tbody>
</div>
<i>
  <%= link_to edit_user_path(current_user.id) do %>
    <i class="fas fa-user-cog"></i>
  <% end %>
</i>

<h2 class="mt-3">New book</h2>
<%= form_with model: Book.new do |f| %>
  <h4>title</h4>
  <%= f.text_field :title %>

  <h4>opinion</h4>
  <%= f.text_area :body %><br>

  <%= f.submit 'Create Book' %>
<% end %>

<h1>ここがホントのbooks/show</h1>

<span>
    <%= link_to book, book_path(book.id) %>
</span>

<h2>Book detail</h2>
<div>
  <%= image_tag @user.get_profile_image(100,100) %>
</div>
<tbody>
  <tr>
    <th>name</th>
    <th><%= @user.name %></th><br>
  </tr>
  <tr>
    <th>introduction</th>
    <th><%= @user.introduction %></th>
  </tr>
</tbody>